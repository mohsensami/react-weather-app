import { Outlet } from 'react-router-dom';
import Header from '../layouts/Header';

const Home = () => {
    return (
        <div>
            <h1>Home</h1>
        </div>
    );
};

export default Home;
